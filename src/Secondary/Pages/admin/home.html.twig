{% extends 'admin/template.html.twig' %}

{% block content %}
    <style>
        table {
            border-collapse: collapse;
        }

        tr, th, td, table {
            border: 1px solid black;
        }

        td {
            padding: 5px;
        }

        .message {
            color: orange;
        }

        .errors {
            color: darkred;
        }
    </style>

    <div class="message">{{ message }}</div>
    <div class="errors">{{ errors }}</div>

    <h1>Домашняя страница Админа</h1>

{% endblock %}
